User: @file:gplat  现在发现一个问题，当某个客户端在同一个gpalt连接上订阅了某个tag，一旦执行writeb写入这个tag时便会失去响应，不仅当前客户连接失去响应，其它的gplat接也会读写tag无应答，仿佛整个gplat服务都不工作了，请仔细排查原因

GitHub Copilot: Updated todo list

我先沿着 `writeb` 服务端处理、订阅通知和锁的持有顺序做一次定点排查，重点确认是否在写标签时同步回发订阅消息造成重入、阻塞或锁未释放。

Searched for regex `WriteB|WRITEB|Notify|Post|subscribe|Subscribe` (`**/gplat/**`), 120 results

Read [](file:///home/gyb/gPlat/gplat/CSubscribe.h)

Read [](file:///home/gyb/gPlat/gplat/ngx_c_threadpool.cxx)

Read [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx#1-1), lines 1 to 700

已经定位到一个可复现的本地死锁路径：`HandleWriteB()` 持有当前连接的 `logicPorcMutex` 后调用 `NotifySubscriber()`；若该连接正是订阅者，通知函数会再次锁同一个非递归 mutex，线程永久阻塞。连续请求会逐步占满线程池，因此最终表现为所有连接都无应答；我再核对互斥量实现和相邻写入路径，然后做最小修复并编译验证。

Read [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx#590-590), lines 590 to 770

Read [](file:///home/gyb/gPlat/gplat/ngx_c_lockmutex.h)

Read [](file:///home/gyb/gPlat/gplat/ngx_c_socket.h#45-45), lines 45 to 180

Read [](file:///home/gyb/gPlat/gplat/ngx_c_socket_conn.cxx#1-1), lines 1 to 120

我先把 `HandleWriteB()` 中互斥量的作用域收窄到“构造并入队写应答”，确保进入订阅通知前已经解锁。这个改动直接打断同连接二次加锁链路，且不改变应答先于事件入队的顺序。

Generating patch (34 lines) in [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx)


```

```
先做一次最窄的可执行验证：仅重编译现有的 gplat，确认锁作用域调整没有引入编译问题。

Execution complete: 验证 gplat 编译

Ran terminal command: cmake --build build/gplat; code=$?; echo "EXIT_CODE:$code"; exit $code

当前环境没有 `cmake`，所以这条验证路径无法启动。我改用仓库现有根目录 Makefile 的具体目标做同范围编译，同时检查相邻的字符串写入和队列写入是否复制了同一死锁模式。

Searched for regex `CLock lock\(&pConn->logicPorcMutex\)|NotifySubscriber\(` (`**/gplat/ngx_c_slogic.cxx`), 24 results

Searched for regex `GetSubscriber\(|GetPlcIoServer\(` (`**/gplat/**`), 5 results

Read [](file:///home/gyb/gPlat/Makefile#1-1), lines 1 to 260

根目录提供了独立的 gplat 目标；现在用 `make gplat` 验证首个修复。随后我会处理搜索确认的两个同构路径，避免 `writeb_string` 或 `writeq` 再触发相同故障。

Execution complete: 编译 gplat 目标

Ran terminal command: make gplat

Created 4 todos

首个修复已通过 `make gplat`。接下来把相同的锁作用域问题一并消除：`writeq`、`writeb_string` 以及两个 PLC 写入处理器都在持有请求连接锁时进入通知流程，结构上存在同样的自锁风险。

Generating patch (123 lines) in [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx)


```

```
补丁工具在重复代码块中自动校正了上下文；先立即编译确认语法完整，再逐段核对它实际收窄的函数，避免遗漏或误落点。

Execution complete: 复编译锁作用域修改

Ran terminal command: make gplat

编译通过。现在核对五个通知调用点前的实际锁作用域，并顺手确认订阅容器快照是否还有可能在并发取消订阅时失效。

Checked [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx), [](file:///home/gyb/gPlat/gplat/CSubscribe.h), no problems found

Read [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx#210-210), lines 210 to 490

Read [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx#890-890), lines 890 to 990

Read [](file:///home/gyb/gPlat/gplat/CSubscribe.h#20-20), lines 20 to 97

核对发现自动校正只命中了 `writeq`，字符串写入和 PLC 写入仍保留旧作用域。我会用函数锚点精确收窄这三个位置；同时把订阅查询改为“持读锁复制快照后返回”，修复当前返回容器引用却已释放读锁的并发失效问题。

Generating patch (118 lines) in [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx), [](file:///home/gyb/gPlat/gplat/CSubscribe.h)


```

```

```

```
相关写入路径和订阅快照都已修正；现在重新编译整个服务目标，确认头文件接口变化及所有调用点一致。

Execution complete: 验证完整服务编译

Ran terminal command: make gplat

Updated todo list

编译已通过。为了验证的不只是“能编译”，我接着找现有客户端测试中是否已经有“同一 socket 先订阅再写入”的流程；若有就直接复用做超时保护下的回归测试。

Searched for regex `subscribe\(|writeb\(|waitpostdata\(|subscribedelaypost\(` (`**/testapp*/**`), 71 results

Searched for regex `WorkerConnections|ProcMsgRecvWorkThreadCount|ListenPort|Daemon` (`**/config/gplat.conf`), 7 results

Searched for regex `int subscribe|bool subscribe|subscribe\(` (`**/higplat/higplat.cpp`), 2 results

Read [](file:///home/gyb/gPlat/config/gplat.conf)

配置里线程池恰好只有 1 个工作线程，这解释了为何第一次自锁就让整个服务立即停摆，而不需要等到多次请求耗尽线程池。现有 testapp2 看起来已经包含同连接订阅后写入的场景，我先确认它是否可安全单独运行，以及服务启动目录和配置要求。

Searched for regex `gplat.conf|QbdFilePath|chdir|CConfig::GetInstance` (`**/gplat/**`), 10 results

Searched for regex `connectgplat\(|disconnectgplat\(|writeb\(|subscribe\(` (`**/include/higplat.h`), 5 results

Read [](file:///home/gyb/gPlat/testapp2/threadfunction.cpp#135-135), lines 135 to 220

Read [](file:///home/gyb/gPlat/testapp2/main.cpp#1-1), lines 1 to 140

现有 testapp2 的事件处理确实会在同一连接上订阅并继续 `writeb`，但它依赖约 1100 个预建标签并执行大范围链式写入，不适合作为无副作用的小回归测试。先检查当前改动差异和所有“持连接锁后通知”的残留模式；运行态验证若当前服务可安全隔离，再用独立端口做。



我现在做最后一轮静态核验：查看实际 diff，并搜索是否还有任一写处理器在 `NotifySubscriber` / `NotifyPlcIoSever` 调用时仍持有 `logicPorcMutex`。

Read changed files in [](file:///home/gyb/gPlat)

Checked , no problems found

Searched for regex `NotifySubscriber\(|NotifyPlcIoSever\(` (`**/gplat/ngx_c_slogic.cxx`), 8 results

diff 暴露出补丁自动校正时误改了两个无关处理器：`HandleReadType` 被错误地重新分配了无包体缓冲区，`HandleCreateQueue` 也被无意义改写。它们虽能编译但会造成行为回归；我现在精确恢复这两段，只保留五个写通知路径的锁修复。

Generating patch (71 lines) in [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx)


```

```
误改已恢复；立即再次编译，之后重新看 diff，确保最终改动只覆盖订阅死锁与订阅快照。

Execution complete: 验证恢复后的编译

Ran terminal command: make gplat

编译通过。现在做最终差异审计，重点确认没有无关函数残留变化，并取准确行号用于说明根因链路。

Read changed files in [](file:///home/gyb/gPlat)

Read [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx#340-340), lines 340 to 390

Read [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx#595-595), lines 595 to 665

Read [](file:///home/gyb/gPlat/gplat/CSubscribe.h#52-52), lines 52 to 99

最终 diff 已干净，只包含预期的五个写通知路径和订阅快照修复。最后检查本机是否已有 gplat 实例占用 8777；若正在运行，我不会擅自重启或对生产数据执行压力测试。

Execution complete: 检查运行中的 gplat

Ran terminal command: echo '--- gplat processes ---' && pgrep -af '(^|/)gplat($| )' || true && echo '--- listening TCP 8777 ---' && ss -ltnp | grep ':8777 ' || true

本机没有运行中的服务或 8777 监听者，因此可以做一次隔离的真实回归：启动刚编译的服务，用 `ctypes` 在同一个 socket 上执行 `subscribe("int1") -> writeb("int1")`，给客户端加 5 秒超时，随后关闭我启动的服务。先确认 `waitpostdata` 的确切 C 接口，以同时验证事件还能取回。

Searched for regex `extern "C".*waitpostdata|waitpostdata\(` (`**/higplat/higplat.cpp`), 1 result

Searched for text `int1` (`**/create_tag.txt`), 101 results

Read [](file:///home/gyb/gPlat/logs)

Read [](file:///home/gyb/gPlat/include/higplat.h#88-88), lines 88 to 115

`waitpostdata` 的 ABI 含 `std::string&`，不适合直接从 Python 调用；但死锁判别只需同 socket 的 `subscribe -> writeb` 能在 5 秒内返回。现在启动服务并确认 worker 已监听，再执行这个最小用例。

Execution complete: 启动回归测试服务

Ran terminal command: set +e; ./gplat; status=$?; echo "EXIT_CODE=$status"; echo '--- gplat processes ---'; pgrep -af '(^|/)gplat($| )' || true; echo '--- listening TCP 8777 ---'; ss -ltnp | grep ':8777' || true

Ran terminal command: set +e; ./gplat; status=$?; echo "EXIT_CODE=$status"; echo '--- gplat processes ---'; pgrep -af '(^|/)gplat($| )' || true; echo '--- listening TCP 8777 ---'; ss -ltnp | grep ':8777' || true

服务已在 8777 正常监听。现在执行最小复现；成功标准是同一 fd 的订阅和写入都返回 `true`，而不是在 `writeb` 等待应答直到 5 秒超时。

Execution complete: 运行同连接订阅写入回归

Ran terminal command: timeout 5s python3 -c 'import ctypes; lib=ctypes.CDLL("./lib/libhigplat.so"); lib.connectgplat.argtypes=[ctypes.c_char_p,ctypes.c_int]; lib.connectgplat.restype=ctypes.c_int; lib.subscribe.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.POINTER(ctypes.c_uint)]; lib.subscribe.restype=ctypes.c_bool; lib.writeb.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.c_void_p,ctypes.c_int,ctypes.POINTER(ctypes.c_uint)]; lib.writeb.restype=ctypes.c_bool; lib.disconnectgplat.argtypes=[ctypes.c_int]; error=ctypes.c_uint(0); fd=lib.connectgplat(b"127.0.0.1",8777); print(f"connect fd={fd}",flush=True); assert fd>=0; subscribed=lib.subscribe(fd,b"int1",ctypes.byref(error)); print(f"subscribe ok={subscribed} error={error.value}",flush=True); assert subscribed; value=ctypes.c_int(12345); written=lib.writeb(fd,b"int1",ctypes.byref(value),ctypes.sizeof(value),ctypes.byref(error)); print(f"writeb ok={written} error={error.value}",flush=True); assert written; lib.disconnectgplat(fd)'
Completed with input: timeout 5s python3 -c 'import ctypes; lib=ctypes.CDLL("./lib/libhigplat.so"); lib.connectgplat.argtypes=[ctypes.c_char_p,ctypes.c_int]; lib.connectgplat.restype=ctypes.c_int; lib.subscribe.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.POINTER(ctypes.c_uint)]; lib.subscribe.restype=ctypes.c_bool; lib.writeb.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.c_void_p,ctypes.c_int,ctypes.POINTER(ctypes.c_uint)]; lib.writeb.restype=ctypes.c_bool; lib.disconnectgplat.argtypes=[ctypes.c_int]; error=ctypes.c_uint(0); fd=lib.connectgplat(b"127.0.0.1",8777); print(f"connect fd={fd}",flush=True); assert fd>=0; subscribed=lib.subscribe(fd,b"int1",ctypes.byref(error)); print(f"subscribe ok={subscribed} error={error.value}",flush=True); assert subscribed; value=ctypes.c_int(12345); written=lib.writeb(fd,b"int1",ctypes.byref(value),ctypes.sizeof(value),ctypes.byref(error)); print(f"writeb ok={written} error={error.value}",flush=True); assert written; lib.disconnectgplat(fd)'

客户端连接返回 `-1`，说明运行态验证尚未成立；先检查刚启动的 worker 是否在 daemon 化后退出、监听端口是否仍存在，并读取日志位置。这个结果不影响已通过的编译验证，但需要把环境问题分清。

Execution complete: 诊断测试服务连接失败

Ran terminal command: echo '== gplat processes ==' && ps -eo pid,ppid,comm,args | grep '[g]plat' || true && echo && echo '== ss -ltnp for 8777 ==' && (ss -ltnp 2>/dev/null | grep ':8777 ' || true) && echo && echo '== log file checks ==' && for f in /home/gyb/gPlat/logs/gplat.log /home/gyb/gPlat/bin/logs/gplat.log; do if [ -e "$f" ]; then echo "EXISTS $f"; echo '--- last 30 lines ---'; tail -n 30 "$f"; echo; else echo "MISSING $f"; fi; done

Starting: *Check related write paths for similar deadlock issues* (4/4)

原配置以 daemon 模式启动后进程自行退出且没有生成日志，属于测试环境启动问题。为不改仓库配置，我会在 `$TMPDIR` 建一个临时运行目录，把 daemon 关闭并改到 18777，前台运行后再测；测试结束会删除临时目录。

Execution complete: 准备隔离测试目录

Ran terminal command: set -e
ROOT="$TMPDIR/gplat-regression"
rm -rf "$ROOT"
mkdir -p "$ROOT/bin" "$ROOT/config" "$ROOT/logs"
ln -s /home/gyb/gPlat/bin/gplat "$ROOT/bin/gplat"
ln -s /home/gyb/gPlat/lib "$ROOT/lib"
ln -s /home/gyb/gPlat/qbdfile "$ROOT/qbdfile"
python3 - <<'PY' "$ROOT/config/gplat.conf" /home/gyb/gPlat/config/gplat.conf
import sys
from pathlib import Path
out_path = Path(sys.argv[1])
in_path = Path(sys.argv[2])
text = in_path.read_text()
text = text.replace('Daemon = 1', 'Daemon = 0')
text = text.replace('ListenPort0 = 8777', 'ListenPort0 = 18777')
out_path.write_text(text)
PY
printf 'DIR=%s\n' "$ROOT"
printf 'Daemon line: '
grep -n '^Daemon = ' "$ROOT/config/gplat.conf" | head -n 1
printf 'ListenPort0 line: '
grep -n '^ListenPort0 = ' "$ROOT/config/gplat.conf" | head -n 1
printf 'ProcMsgRecvWorkThreadCount line: '
grep -n '^ProcMsgRecvWorkThreadCount = ' "$ROOT/config/gplat.conf" | head -n 1


隔离配置准备完成，保持单工作线程以复现原始故障条件。现在前台启动服务，随后在 18777 上执行同连接用例。

Ran terminal command: ./gplat

服务已成功加载 `BOARD` 并保持前台运行。执行回归请求，5 秒超时仍作为死锁检测边界。

Execution complete: 验证同连接写入不死锁

Ran terminal command: timeout 5s python3 -c 'import ctypes; lib=ctypes.CDLL("./lib/libhigplat.so"); lib.connectgplat.argtypes=[ctypes.c_char_p,ctypes.c_int]; lib.connectgplat.restype=ctypes.c_int; lib.subscribe.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.POINTER(ctypes.c_uint)]; lib.subscribe.restype=ctypes.c_bool; lib.writeb.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.c_void_p,ctypes.c_int,ctypes.POINTER(ctypes.c_uint)]; lib.writeb.restype=ctypes.c_bool; lib.disconnectgplat.argtypes=[ctypes.c_int]; error=ctypes.c_uint(0); fd=lib.connectgplat(b"127.0.0.1",18777); print(f"connect fd={fd}",flush=True); assert fd>=0; subscribed=lib.subscribe(fd,b"int1",ctypes.byref(error)); print(f"subscribe ok={subscribed} error={error.value}",flush=True); assert subscribed; value=ctypes.c_int(12345); written=lib.writeb(fd,b"int1",ctypes.byref(value),ctypes.sizeof(value),ctypes.byref(error)); print(f"writeb ok={written} error={error.value}",flush=True); assert written; lib.disconnectgplat(fd)' 2>&1

Ran terminal command: set +e && timeout 5s python3 -c 'import ctypes; lib=ctypes.CDLL("./lib/libhigplat.so"); lib.connectgplat.argtypes=[ctypes.c_char_p,ctypes.c_int]; lib.connectgplat.restype=ctypes.c_int; lib.subscribe.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.POINTER(ctypes.c_uint)]; lib.subscribe.restype=ctypes.c_bool; lib.writeb.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.c_void_p,ctypes.c_int,ctypes.POINTER(ctypes.c_uint)]; lib.writeb.restype=ctypes.c_bool; lib.disconnectgplat.argtypes=[ctypes.c_int]; error=ctypes.c_uint(0); fd=lib.connectgplat(b"127.0.0.1",18777); print(f"connect fd={fd}",flush=True); assert fd>=0; subscribed=lib.subscribe(fd,b"int1",ctypes.byref(error)); print(f"subscribe ok={subscribed} error={error.value}",flush=True); assert subscribed; value=ctypes.c_int(12345); written=lib.writeb(fd,b"int1",ctypes.byref(value),ctypes.sizeof(value),ctypes.byref(error)); print(f"writeb ok={written} error={error.value}",flush=True); assert written; lib.disconnectgplat(fd)' 2>&1; status=$?; echo "EXIT_CODE=$status"

前台进程仍显示为运行，但 18777 拒绝连接，说明监听配置可能没有按预期生效，或 worker 已在后台退出。先查实际监听端口与进程树，不继续猜测。

Execution complete: 检查隔离服务监听状态

Ran terminal command: set -e
printf '== gplat processes ==\n'
ps -eo pid=,ppid=,stat=,comm=,args= | awk 'tolower($4) ~ /gplat/ || tolower($5) ~ /gplat/ {print}' || true
printf '\n== TCP listeners (ports 8777/18777 or owned by gplat) ==\n'
(ss -ltnp 2>/dev/null || true) | grep -E ':(8777|18777)\b|gplat' || true
printf '\n== last 40 lines of %s ==\n' "${TMPDIR:-/tmp}/gplat-regression/logs/gplat.log"
if [ -f "${TMPDIR:-/tmp}/gplat-regression/logs/gplat.log" ]; then
  tail -n 40 "${TMPDIR:-/tmp}/gplat-regression/logs/gplat.log"
else
  echo '(file not found)'
fi


原因是终端服务与执行子代理处在不同的网络隔离环境，日志证明服务确实监听过 18777，但客户端从另一个隔离环境无法访问。改为在同一个终端命令里启动、测试并清理服务，就能得到有效结果；先结束当前测试终端。

Killed terminal

现在把服务、就绪探测和客户端放进同一个终端沙箱执行；仍保持单工作线程，测试完成后用 `SIGTERM` 正常回收服务。

Ran terminal command: cd "$TMPDIR/gplat-regression/bin" && ./gplat >"$TMPDIR/gplat-regression/server.stdout" 2>&1 &
server_pid=$!
ready=0
for attempt in {1..100000}; do
	if : >/dev/tcp/127.0.0.1/18777 2>/dev/null; then
		ready=1
		break
	fi
done
if [[ "$ready" -ne 1 ]]; then
	echo "service readiness failed"
	kill -TERM "$server_pid" 2>/dev/null
	wait "$server_pid" 2>/dev/null
	exit 1
fi
timeout 5s python3 -c 'import ctypes; lib=ctypes.CDLL("/home/gyb/gPlat/lib/libhigplat.so"); lib.connectgplat.argtypes=[ctypes.c_char_p,ctypes.c_int]; lib.connectgplat.restype=ctypes.c_int; lib.subscribe.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.POINTER(ctypes.c_uint)]; lib.subscribe.restype=ctypes.c_bool; lib.writeb.argtypes=[ctypes.c_int,ctypes.c_char_p,ctypes.c_void_p,ctypes.c_int,ctypes.POINTER(ctypes.c_uint)]; lib.writeb.restype=ctypes.c_bool; lib.disconnectgplat.argtypes=[ctypes.c_int]; error=ctypes.c_uint(0); fd=lib.connectgplat(b"127.0.0.1",18777); print(f"connect fd={fd}",flush=True); assert fd>=0; subscribed=lib.subscribe(fd,b"int1",ctypes.byref(error)); print(f"subscribe ok={subscribed} error={error.value}",flush=True); assert subscribed; value=ctypes.c_int(12345); written=lib.writeb(fd,b"int1",ctypes.byref(value),ctypes.sizeof(value),ctypes.byref(error)); print(f"writeb ok={written} error={error.value}",flush=True); assert written; lib.disconnectgplat(fd)'
test_status=$?
kill -TERM "$server_pid" 2>/dev/null
wait "$server_pid" 2>/dev/null
exit "$test_status"

真实回归已通过：单工作线程下，同一 fd 的 `subscribe("int1")` 和 `writeb("int1")` 都立即成功返回，错误码为 0。测试服务已正常结束；我清理临时目录并做最后一次进程、构建产物与工作区检查。注意该回归按请求实际写入了测试板中的 `int1=12345`。

Execution complete: 清理并终检回归环境

Ran terminal command: TMPDIR="${TMPDIR:-/home/gyb/.vscode-server-insiders/tmp/tmp_vscode_3}"; target="$TMPDIR/gplat-regression"; if [ -e "$target" ]; then rm -rf -- "$target"; echo "removed:$target"; else echo "absent:$target"; fi; echo; echo "gplat processes:"; pgrep -af '[g]plat' || true; echo; echo "listeners:"; ss -ltn 2>/dev/null | grep -E ':(8777|18777) ' || true; if command -v lsof >/dev/null 2>&1; then echo; echo "lsof:"; lsof -nP -iTCP:8777 -sTCP:LISTEN 2>/dev/null || true; lsof -nP -iTCP:18777 -sTCP:LISTEN 2>/dev/null || true; fi

Ran terminal command: echo "exact-name:"; pgrep -x gplat || true; echo; echo "cmdline-match:"; ps -eo pid=,comm=,args= | grep -E '[g]plat' | grep -v grep || true; echo; echo "port-check:"; ss -ltn 2>/dev/null | grep -E ':(8777|18777) ' || true

临时环境已清理，未留下服务进程或监听端口。我再确认最终工作区只有预期源码变化，并记录这次运行态测试应复制 QBD 数据文件而非链接原文件的经验，避免后续测试触碰现有板数据。

Read changed files in [](file:///home/gyb/gPlat)

Checked [](file:///home/gyb/gPlat/gplat/CSubscribe.h), [](file:///home/gyb/gPlat/gplat/ngx_c_slogic.cxx), no problems found

Read memory

Created memory file [](file:///home/gyb/.vscode-server-insiders/data/User/workspaceStorage/57d392e2d9a9e4ac3d1e245a27cdf9c1/GitHub.copilot-chat/memory-tool/memories/repo/testing.md)

**根因**

ngx_c_slogic.cxx 持有当前连接的 `logicPorcMutex` 时调用 `NotifySubscriber()`。当写入连接本身也是订阅者时，ngx_c_slogic.cxx 再次锁同一个非递归 mutex，形成永久自锁。

配置中只有一个工作线程，因此该线程阻塞后，所有连接的请求都无法处理。

**已修复**

- 在通知订阅者前释放连接锁。
- 同步修复 `writeq`、字符串写入和 PLC 写入的同类问题。
- CSubscribe.h 改为在读锁内复制订阅快照，避免返回已失去锁保护的容器引用。

验证结果：

- `make gplat` 编译成功。
- 单工作线程真实测试通过：同一连接执行 `subscribe("int1") -> writeb("int1")`，两步均立即返回成功，错误码为 `0`。
- 测试服务和临时文件已清理。

注意：运行态测试将当前测试板中的 `int1` 写成了 `12345`。

Completed: *Check related write paths for similar deadlock issues* (4/4)

Made changes.